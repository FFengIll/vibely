{
  "name": "remote-vibe",
  "version": "0.1.0",
  "description": "Remote task dispatcher for vibe coding tools",
  "type": "module",
  "main": "./src/index.ts",
  "bin": {
    "remote-vibe": "./src/cli/index.ts"
  },
  "scripts": {
    "dev": "bun run src/cli/index.ts",
    "start": "bun run src/cli/index.ts",
    "test": "bun test",
    "build": "bun build src/cli/index.ts --outdir ./dist --target bun"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
